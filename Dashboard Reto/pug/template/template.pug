block config
    include ../config/config.pug

<!DOCTYPE html>
html
    head
        title
        meta(charset= "utf-8")
        meta(name="viewport" content="width=device-width, user-scalable=no, initial-scale=1, maximun-scale=1, minimun-scale=1")
        link(rel="stylesheet" href="css/styles.css")

    body(class=currentPage_classbody)
        block header
        block sidebar
        block main

        #overlay-content

        .plus-options
            span.icon-plus

        script(type="text/javascript" src="js/Chart.min.js")
        script(type="text/javascript" src="js/_charts.js")
        script(type="text/javascript" src="js/_menu.js")
